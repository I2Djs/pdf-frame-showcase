{
  "name": "@i2d/pdf-frame-showcase",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "postversion": "git tag pdf-frame-showcase-$(node -p -e \"require('./package.json').version\") && git push origin pdf-frame-showcase-$(node -p -e \"require('./package.json').version\") && git tag -d v$(node -p -e \"require('./package.json').version\") && git push origin :refs/tags/v$(node -p -e \"require('./package.json').version\")"
  },
  "dependencies": {
    "@i2d/nuxt-pdf-frame": "^0.5.0",
    "@mdi/font": "^7.4.47",
    "@mdi/js": "^7.4.47",
    "@nuxt/content": "^2.12.1",
    "d3-array": "latest",
    "d3-geo": "^3.1.1",
    "d3-scale": "latest",
    "d3-shape": "^3.2.0",
    "d3-time-format": "latest",
    "nuxt": "^3.11.1",
    "vue": "^3.4.21",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "shiki": "^1.10.1",
    "vite-plugin-vuetify": "^2.0.3",
    "vuetify": "^3.5.10",
    "vuetify-nuxt-module": "^0.13.1"
  },
  "version": "0.1.0"
}
